import React, { useEffect, useRef, useState } from 'react';
import { motion, useScroll, useTransform, useSpring, AnimatePresence } from 'framer-motion';
import { Sparkles, Diamond, X, Loader2 } from 'lucide-react';

// --- Gemini API 设置 ---
// 运行时环境会自动提供真实的 API Key，这里保持为空字符串即可
const apiKey = "";

// --- 作品集数据 ---
const PORTFOLIO_DATA = {
  // 1. AI 视觉实验 (复古迪斯科海报)
  posters: [
    { id: 1, title: "DISCO FEVER", subtitle: "Retro Typography & Vinyl", src: "PORTFOLIO banner 1.jpg" },
    { id: 2, title: "CHERRY BOMB", subtitle: "Surreal Collage", src: "PORTFOLIO banner 5.jpg" },
    { id: 3, title: "FLORAL ILLUSION", subtitle: "Nature Meets Digital", src: "PORTFOLIO banner 3 (1).jpg" },
    { id: 4, title: "70S AESTHETIC", subtitle: "Classic Layout", src: "PORTFOLIO banner 4.jpg" },
    { id: 5, title: "NEON BLOSSOM", subtitle: "Floral & Disco Concept", src: "PORTFOLIO banner 2.jpg" }
  ],
  // 2. 东方意象与产品化 (插画/丝巾/包装)
  culturalIP: [
    { id: 11, title: "圆明园 IP 丝巾", desc: "从平铺插画到丝巾织物的商业转化", src: "IMG_4032.jpg", colSpan: "col-span-12 md:col-span-8" },
    { id: 12, title: "龙与水榭", desc: "东方神话重构", src: "IMG_4039.jpg", colSpan: "col-span-12 md:col-span-4" },
    { id: 13, title: "节气礼盒", desc: "文创包装设计", src: "IMG_4027.jpg", colSpan: "col-span-12 md:col-span-4" },
    { id: 14, title: "白虎与枯山水", desc: "色彩阵列探索", src: "IMG_4038.jpg", colSpan: "col-span-12 md:col-span-8" },
    { id: 15, title: "柿柿如意", desc: "童趣与传统元素结合", src: "IMG_4037.jpg", colSpan: "col-span-12 md:col-span-6" },
    { id: 16, title: "丝巾实物展开", desc: "成品视觉呈现", src: "IMG_4034.jpg", colSpan: "col-span-12 md:col-span-6" }
  ],
  // 3. 高定珠宝手绘
  jewelry: [
    { id: 21, title: "Emerald Heritage", desc: "祖母绿切割与碎钻镶嵌，展现极致光影细节。", src: "IMG_4017.jpg" },
    { id: 22, title: "Radiant Sun Ring", desc: "放射状结构设计与玫瑰金质感表现。", src: "IMG_4015.jpg" },
    { id: 23, title: "Diamond River", desc: "高定项链手绘，捕捉宝石切面的折射率。", src: "IMG_4011.jpg" }
  ]
};

// --- 自定义跟随鼠标 ---
const CustomCursor = () => {
  const [mousePosition, setMousePosition] = useState({ x: 0, y: 0 });
  const [isHovering, setIsHovering] = useState(false);

  useEffect(() => {
    const updateMousePosition = (e) => setMousePosition({ x: e.clientX, y: e.clientY });
    const handleMouseOver = (e) => {
      if (e.target.closest('.hover-trigger') || e.target.closest('button')) setIsHovering(true);
      else setIsHovering(false);
    };

    window.addEventListener('mousemove', updateMousePosition);
    window.addEventListener('mouseover', handleMouseOver);
    return () => {
      window.removeEventListener('mousemove', updateMousePosition);
      window.removeEventListener('mouseover', handleMouseOver);
    };
  }, []);

  return (
    <motion.div
      className="fixed top-0 left-0 bg-white rounded-full pointer-events-none z-[999] mix-blend-difference hidden md:block"
      animate={{
        x: mousePosition.x - (isHovering ? 40 : 10),
        y: mousePosition.y - (isHovering ? 40 : 10),
        width: isHovering ? 80 : 20,
        height: isHovering ? 80 : 20,
      }}
      transition={{ type: "spring", stiffness: 500, damping: 28, mass: 0.5 }}
    >
      <AnimatePresence>
        {isHovering && (
          <motion.div 
            initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }}
            className="w-full h-full flex items-center justify-center text-black font-bold text-xs mix-blend-normal uppercase tracking-widest"
          >
            Explore
          </motion.div>
        )}
      </AnimatePresence>
    </motion.div>
  );
};

// --- 横向滚动区域组件 (用于复古海报) ---
const HorizontalScrollSection = () => {
  const targetRef = useRef(null);
  const { scrollYProgress } = useScroll({ target: targetRef });
  
  const x = useTransform(scrollYProgress, [0, 1], ["5%", "-80%"]);
  const textX = useTransform(scrollYProgress, [0, 1], ["0%", "15%"]);

  return (
    <section ref={targetRef} className="relative h-[500vh] bg-[#050505]">
      <div className="sticky top-0 h-screen flex items-center overflow-hidden">
        
        <motion.div style={{ x: textX }} className="absolute whitespace-nowrap opacity-[0.03] z-0 pointer-events-none">
          <h2 className="text-[25vw] font-black uppercase italic tracking-tighter">RETRO DISCO AI LAB RETRO DISCO</h2>
        </motion.div>

        <motion.div style={{ x }} className="flex gap-12 md:gap-24 px-[10vw] z-10">
          {PORTFOLIO_DATA.posters.map((poster, index) => (
            <div key={poster.id} className="relative group w-[85vw] md:w-[50vw] h-[60vh] md:h-[75vh] shrink-0 hover-trigger">
              <div className="w-full h-full overflow-hidden relative rounded-sm">
                <motion.img 
                  src={poster.src} 
                  alt={poster.title}
                  className="w-full h-full object-cover grayscale brightness-50 group-hover:grayscale-0 group-hover:brightness-100 group-hover:scale-[1.05] transition-all duration-700 ease-out"
                />
                <div className="absolute inset-0 bg-red-600/0 group-hover:bg-red-600/10 mix-blend-overlay transition-colors duration-500" />
              </div>
              <div className="absolute -bottom-10 left-0 md:-left-8 mix-blend-difference pointer-events-none z-20">
                <h3 className="text-5xl md:text-7xl font-black text-white tracking-tighter uppercase leading-none">{poster.title}</h3>
                <p className="text-zinc-300 font-serif italic text-lg md:text-xl mt-2">{poster.subtitle}</p>
              </div>
            </div>
          ))}
        </motion.div>
      </div>
    </section>
  );
};

// --- 跑马灯组件 ---
const Marquee = ({ text, reverse }) => {
  return (
    <div className="relative flex overflow-x-hidden w-full py-4 bg-zinc-900 border-y border-zinc-800 -rotate-3 scale-110 z-20">
      <div className={`animate-marquee whitespace-nowrap flex items-center ${reverse ? 'reverse' : ''}`}>
        {[...Array(4)].map((_, i) => (
          <span key={i} className="text-zinc-400 hover:text-zinc-100 transition-colors duration-300 text-2xl md:text-4xl font-bold uppercase tracking-widest px-8 flex items-center">
            {text} <Sparkles className="inline ml-8 text-red-600" size={24} />
          </span>
        ))}
      </div>
      <style>{`
        .animate-marquee { animation: marquee 30s linear infinite; }
        .animate-marquee.reverse { animation-direction: reverse; }
        @keyframes marquee { 0% { transform: translateX(0%); } 100% { transform: translateX(-50%); } }
      `}</style>
    </div>
  );
};

export default function App() {
  const { scrollYProgress } = useScroll();
  const smoothProgress = useSpring(scrollYProgress, { damping: 20, mass: 0.5, stiffness: 100 });
  const headerY = useTransform(smoothProgress, [0, 0.2], [0, -150]);
  const opacityHero = useTransform(smoothProgress, [0, 0.15], [1, 0]);

  // --- Gemini API 状态管理 ---
  const [isModalOpen, setIsModalOpen] = useState(false);
  const [selectedProject, setSelectedProject] = useState(null);
  const [aiAnalysis, setAiAnalysis] = useState("");
  const [isGenerating, setIsGenerating] = useState(false);
  const [aiError, setAiError] = useState("");

  // 处理调用 Gemini API 并带有指数退避重试逻辑
  const fetchAIAnalysis = async (project) => {
    setSelectedProject(project);
    setIsModalOpen(true);
    setAiAnalysis("");
    setAiError("");
    setIsGenerating(true);

    const prompt = `你是一位顶尖的艺术评论家与时尚杂志主编。请根据以下作品信息，为独立设计师林子熙(ZiXi Lin)的作品写一段引人入胜的、具有先锋感的设计解析。
    要求：
    1. 语气高级、专业、带有艺术鉴赏力。
    2. 字数控制在150字左右。
    3. 挖掘作品名称和描述背后的东方美学或工艺美感。
    
    作品名称：${project.title}
    作品简介：${project.desc}`;

    const maxRetries = 5;
    const delays = [1000, 2000, 4000, 8000, 16000];
    
    for (let attempt = 0; attempt < maxRetries; attempt++) {
      try {
        const response = await fetch(
          `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-preview-09-2025:generateContent?key=${apiKey}`,
          {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({
              contents: [{ parts: [{ text: prompt }] }]
            })
          }
        );

        if (!response.ok) {
          throw new Error(`HTTP error! status: ${response.status}`);
        }

        const data = await response.json();
        const generatedText = data.candidates?.[0]?.content?.parts?.[0]?.text;
        
        if (generatedText) {
          setAiAnalysis(generatedText);
          setIsGenerating(false);
          return;
        } else {
           throw new Error("No text generated");
        }
      } catch (err) {
        if (attempt === maxRetries - 1) {
          setAiError("AI 灵感引擎暂时需要休息，请稍后再试，或继续欣赏其他作品。");
          setIsGenerating(false);
        } else {
          await new Promise(resolve => setTimeout(resolve, delays[attempt]));
        }
      }
    }
  };

  return (
    <div className="bg-[#050505] min-h-screen text-zinc-100 font-sans selection:bg-red-600 selection:text-white cursor-none overflow-x-hidden">
      
      {/* 噪点背景 */}
      <div className="fixed inset-0 pointer-events-none z-[90] opacity-[0.04] mix-blend-screen" style={{ backgroundImage: `url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.8' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)'/%3E%3C/svg%3E")` }}></div>

      <CustomCursor />

      {/* --- AI 策展人模态框 --- */}
      <AnimatePresence>
        {isModalOpen && selectedProject && (
          <motion.div 
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            exit={{ opacity: 0 }}
            className="fixed inset-0 z-[200] flex items-center justify-center p-4 md:p-10"
          >
            <div className="absolute inset-0 bg-black/80 backdrop-blur-md" onClick={() => setIsModalOpen(false)} />
            <motion.div 
              initial={{ y: 50, scale: 0.95 }}
              animate={{ y: 0, scale: 1 }}
              exit={{ y: 20, scale: 0.95 }}
              className="relative bg-zinc-950 border border-zinc-800 p-8 md:p-12 w-full max-w-2xl rounded-sm shadow-[0_0_50px_rgba(220,38,38,0.1)]"
            >
              <button 
                onClick={() => setIsModalOpen(false)}
                className="absolute top-6 right-6 text-zinc-500 hover:text-white transition-colors"
              >
                <X size={24} />
              </button>
              
              <div className="flex items-center gap-3 mb-8 border-b border-zinc-800 pb-4">
                <Sparkles className="text-red-600" size={24} />
                <h3 className="text-xl font-bold uppercase tracking-widest text-zinc-300">AI Curator Review</h3>
              </div>
              
              <h4 className="text-3xl font-black mb-2">{selectedProject.title}</h4>
              <p className="text-zinc-500 text-sm mb-8 font-light">{selectedProject.desc}</p>
              
              <div className="bg-[#0a0a0a] p-6 rounded-sm min-h-[150px] relative overflow-hidden border border-zinc-900">
                {isGenerating ? (
                  <div className="absolute inset-0 flex flex-col items-center justify-center text-zinc-500 gap-4">
                    <Loader2 className="animate-spin text-red-600" size={32} />
                    <span className="text-sm tracking-widest uppercase animate-pulse">Gemini 正在解析作品美学...</span>
                  </div>
                ) : aiError ? (
                  <p className="text-red-500/80 text-center mt-8">{aiError}</p>
                ) : (
                  <motion.p 
                    initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ duration: 0.5 }}
                    className="text-zinc-300 leading-relaxed font-serif text-lg text-justify"
                  >
                    {aiAnalysis}
                  </motion.p>
                )}
              </div>
            </motion.div>
          </motion.div>
        )}
      </AnimatePresence>

      {/* --- 首屏 Hero 区 --- */}
      <motion.section 
        style={{ opacity: opacityHero }}
        className="relative h-screen flex flex-col items-center justify-center px-6"
      >
        <motion.div 
          initial={{ opacity: 0, scale: 0.8 }}
          animate={{ opacity: 1, scale: 1 }}
          transition={{ duration: 1, ease: "easeOut" }}
          className="absolute top-12 left-6 md:left-12 flex items-center gap-4"
        >
          <img src="231775800587_.pic_hd.jpg" alt="Profile" className="w-12 h-12 rounded-full grayscale border border-zinc-800" />
          <span className="text-xs tracking-[0.2em] uppercase text-zinc-500 font-medium">ZI_XI <br/>Fashion Design</span>
        </motion.div>

        <motion.div style={{ y: headerY }} className="text-center z-10 flex flex-col items-center mt-12">
          <motion.div 
            initial={{ scale: 0.8, opacity: 0 }} animate={{ scale: 1, opacity: 1 }} transition={{ duration: 1.5, ease: "easeOut" }}
            className="mb-8 flex justify-center"
          >
            <div className="px-5 py-2 border border-zinc-800 rounded-full text-xs md:text-sm tracking-[0.3em] uppercase text-zinc-400 flex items-center gap-3">
              <span className="w-2 h-2 bg-red-600 rounded-full animate-pulse shadow-[0_0_10px_rgba(220,38,38,0.8)]"></span>
              Avant-Garde Designer
            </div>
          </motion.div>

          <h1 className="text-[14vw] md:text-[11vw] leading-[0.8] font-black tracking-tighter uppercase flex flex-col items-center">
            <motion.span initial={{ y: 100, opacity: 0 }} animate={{ y: 0, opacity: 1 }} transition={{ duration: 1, delay: 0.1 }} className="block overflow-hidden">
              <span className="block">林子熙</span>
            </motion.span>
            <motion.span initial={{ y: 100, opacity: 0 }} animate={{ y: 0, opacity: 1 }} transition={{ duration: 1, delay: 0.3 }} className="block text-red-600 italic font-serif mt-2">
              Creative
            </motion.span>
            <motion.span initial={{ y: 100, opacity: 0 }} animate={{ y: 0, opacity: 1 }} transition={{ duration: 1, delay: 0.5 }} className="block overflow-hidden mt-2">
              <span className="block">Visionary.</span>
            </motion.span>
          </h1>
          
          <motion.p initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ delay: 1, duration: 1 }} className="mt-12 text-zinc-400 max-w-xl text-center text-sm md:text-base font-light leading-relaxed">
            服装与服饰设计科班出身。打破边界，通过 AI 工具颠覆传统创作流，<br className="hidden md:block"/>
            将东方意象与 70s 复古美学转译为极具张力的现代时尚视觉语言。
          </motion.p>
        </motion.div>

        <motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ delay: 1.5, duration: 1 }} className="absolute bottom-8 left-1/2 -translate-x-1/2 flex flex-col items-center gap-4 text-zinc-600 text-xs tracking-[0.3em] uppercase">
          Scroll to explore
          <motion.div 
            animate={{ y: [0, 15, 0] }}
            transition={{ repeat: Infinity, duration: 2, ease: "easeInOut" }}
            className="w-[1px] h-16 bg-gradient-to-b from-zinc-500 to-transparent"
          ></motion.div>
        </motion.div>
      </motion.section>

      {/* --- 跑马灯：履历展示 --- */}
      <section className="pt-24 pb-12 bg-[#050505] overflow-hidden">
        <Marquee text="2024 设计马拉松金奖 · 万科活动策划 · ChatGPT 灵感提效 · Midjourney 视觉生成 · Claude 逻辑梳理" reverse={false} />
        <div className="mt-6">
          <Marquee text="Dalian Polytechnic University · Fashion & Accessory Design · Core Competitiveness · AI Empowerment" reverse={true} />
        </div>
      </section>

      {/* --- 横向滚动区：复古海报 --- */}
      <HorizontalScrollSection />

      {/* --- 不规则网格区：东方意象与商业落地 --- */}
      <section className="py-32 md:py-48 px-6 md:px-12 lg:px-24 bg-zinc-950">
        <div className="mb-24 md:flex justify-between items-end border-b border-zinc-800 pb-12">
          <div>
            <h2 className="text-5xl md:text-7xl font-black uppercase tracking-tighter mb-4 leading-none">Cultural IP <br/><span className="text-zinc-600 font-serif italic font-light">& Textile</span></h2>
            <p className="text-zinc-400 max-w-md font-light text-lg mt-6">将传统的平铺插画、东方神话与节气意象，通过严谨的设计逻辑转化为丝巾织物与文创包装，兼顾审美深度与商业落地。</p>
          </div>
        </div>

        <div className="grid grid-cols-12 gap-4 md:gap-8 auto-rows-[250px] md:auto-rows-[350px]">
          {PORTFOLIO_DATA.culturalIP.map((item) => (
            <motion.div 
              initial={{ opacity: 0, y: 50 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true, margin: "-100px" }}
              transition={{ duration: 0.8 }}
              key={item.id} 
              className={`${item.colSpan} relative group hover-trigger overflow-hidden bg-zinc-900 rounded-sm`}
            >
              <img 
                src={item.src} 
                alt={item.title} 
                className="w-full h-full object-cover opacity-50 grayscale-[50%] group-hover:opacity-100 group-hover:grayscale-0 group-hover:scale-[1.03] transition-all duration-700 ease-[cubic-bezier(0.25,1,0.5,1)]" 
              />
              
              <div className="absolute inset-0 p-6 md:p-10 flex flex-col justify-end bg-gradient-to-t from-black/90 via-black/40 to-transparent translate-y-8 opacity-0 group-hover:translate-y-0 group-hover:opacity-100 transition-all duration-500">
                <h3 className="text-2xl md:text-4xl font-bold mb-2 tracking-tight">{item.title}</h3>
                <p className="text-zinc-300 text-sm md:text-base font-light mb-6">{item.desc}</p>
                
                {/* ✨ Gemini API 触发按钮 */}
                <button 
                  onClick={(e) => { e.stopPropagation(); fetchAIAnalysis(item); }}
                  className="flex items-center gap-2 w-fit px-4 py-2 bg-red-600/90 hover:bg-red-500 text-white text-xs uppercase tracking-widest font-bold rounded-sm transition-colors"
                >
                  <Sparkles size={14} /> AI 深度解析
                </button>
              </div>
            </motion.div>
          ))}
        </div>
      </section>

      {/* --- 聚光灯展示区：高定珠宝手绘 --- */}
      <section className="py-32 md:py-48 px-6 md:px-12 lg:px-24 bg-[#080808]">
        <div className="mb-24 text-center flex flex-col items-center">
          <Diamond className="text-red-600 mb-8" size={48} strokeWidth={1} />
          <h2 className="text-5xl md:text-7xl font-serif italic mb-4 leading-none text-zinc-300">High Jewelry</h2>
          <h2 className="text-5xl md:text-7xl font-black uppercase tracking-tighter mb-6 leading-none">Archives.</h2>
          <p className="text-zinc-500 uppercase tracking-[0.2em] text-sm md:text-base">硬核手绘功底 / 光影细节捕捉</p>
        </div>

        <div className="flex flex-col gap-24 md:gap-40">
          {PORTFOLIO_DATA.jewelry.map((item, i) => (
            <motion.div 
              initial={{ opacity: 0, y: 100 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true, margin: "-10%" }}
              transition={{ duration: 1.2, ease: "easeOut" }}
              key={item.id} 
              className={`flex flex-col md:flex-row items-center gap-12 md:gap-24 ${i % 2 !== 0 ? 'md:flex-row-reverse' : ''}`}
            >
              <div className="w-full md:w-3/5 relative group hover-trigger">
                <div className="w-full aspect-square md:aspect-[4/3] bg-zinc-950 overflow-hidden relative rounded-sm shadow-[0_20px_50px_rgba(0,0,0,0.5)]">
                  <img 
                    src={item.src} 
                    alt={item.title} 
                    className="w-full h-full object-cover opacity-30 grayscale brightness-[0.2] contrast-125 group-hover:opacity-100 group-hover:grayscale-0 group-hover:brightness-100 group-hover:contrast-100 group-hover:scale-105 transition-all duration-[1200ms] ease-[cubic-bezier(0.19,1,0.22,1)]" 
                  />
                  <div className="absolute inset-0 bg-gradient-to-tr from-transparent via-white/5 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-1000 pointer-events-none"></div>
                </div>
              </div>
              
              <div className="w-full md:w-2/5 flex flex-col justify-center items-start">
                <div className="text-red-600 text-sm md:text-base font-mono mb-6 border-b border-zinc-800 pb-2 inline-block w-fit">0{i+1} / 03</div>
                <h3 className="text-4xl md:text-5xl lg:text-6xl font-black uppercase tracking-tight mb-6 leading-none">{item.title}</h3>
                <p className="text-zinc-400 leading-relaxed text-lg font-light mb-8">
                  {item.desc} 扎实的科班手绘基本功，精准捕捉宝石切面的折射率与金属的高光质感。
                </p>
                
                {/* ✨ Gemini API 触发按钮 */}
                <button 
                  onClick={() => fetchAIAnalysis(item)}
                  className="flex items-center gap-2 px-6 py-3 border border-zinc-700 hover:border-red-600 hover:text-red-500 text-zinc-300 text-sm uppercase tracking-widest transition-all duration-300"
                >
                  <Sparkles size={16} /> 让 AI 解析灵感美学
                </button>
              </div>
            </motion.div>
          ))}
        </div>
      </section>

      {/* --- Footer 联系区 --- */}
      <footer className="relative py-40 px-6 text-center overflow-hidden border-t border-zinc-900 bg-[#020202]">
        <motion.div 
          initial={{ y: 50, opacity: 0 }}
          whileInView={{ y: 0, opacity: 1 }}
          viewport={{ once: true }}
          className="relative z-10 hover-trigger"
        >
          <p className="text-zinc-500 uppercase tracking-[0.3em] text-sm mb-8">Ready for new challenges</p>
          <h2 className="text-[18vw] md:text-[12vw] leading-[0.8] font-black tracking-tighter uppercase text-zinc-100 hover:text-red-600 transition-colors duration-700 cursor-pointer inline-block">
            Let's <br className="md:hidden" />Create.
          </h2>
          
          <div className="mt-16 flex flex-col md:flex-row justify-center items-center gap-6 md:gap-12 text-zinc-400 uppercase tracking-widest text-xs md:text-sm font-medium">
            <a href="mailto:Sylus00100@outlook.com" className="hover:text-white transition-colors border-b border-transparent hover:border-white pb-1 flex items-center gap-2">
              Email: Sylus00100@outlook.com
            </a>
            <span className="hidden md:block w-1 h-1 bg-zinc-700 rounded-full"></span>
            <span className="flex items-center gap-2">WeChat: MLIU_SISMO</span>
            <span className="hidden md:block w-1 h-1 bg-zinc-700 rounded-full"></span>
            <span className="flex items-center gap-2">Tel: 17741368088</span>
          </div>
          
          <div className="mt-24 text-zinc-700 text-xs font-mono uppercase tracking-widest">
            © {new Date().getFullYear()} ZiXi Lin. All Rights Reserved. Powered by Gemini AI.
          </div>
        </motion.div>
      </footer>

    </div>
  );
}
